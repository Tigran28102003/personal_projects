"""Direction A — Vol-Engine: RV forecasting (HAR family + ML) + vol-timing overlay; VRP backlog.

Standalone package at the repo root; reuses the legacy library in the sibling ACTUAL_VERSION and
the perp-era exo snapshot in the sibling v2_microstructure. See the package README and the plan.
"""
