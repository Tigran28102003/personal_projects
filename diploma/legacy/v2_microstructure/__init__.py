"""Redesign (direction C): microstructure/flow + triple-barrier + meta-label.

A self-contained core that imports the existing pipeline at REPO_ROOT as a library.
See ``REPO_ROOT/REDESIGN_BRIEF.md`` (single source of truth) and the package README.
"""
