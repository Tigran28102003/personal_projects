# ============================================================
# requirements.txt — прогнозирование ВРП субъектов РФ
# Протестировано на Python 3.10–3.12
# ============================================================

# --- Обработка данных ---
numpy>=1.26
pandas>=2.1
openpyxl>=3.1          # чтение Excel-файлов Росстата
lxml>=5.0              # pd.read_html для таблиц ЦБ РФ
scipy>=1.11

# --- Базовые модели ---
scikit-learn>=1.4      # Ridge, Lasso, ElasticNet, RandomForest, стэкинг
statsmodels>=0.14      # AR(1), OLS с кластерными SE
linearmodels>=6.0      # панельные модели с FE/RE (PanelOLS, BetweenOLS, PooledOLS)

# --- Ансамблевые модели ---
xgboost>=2.0
lightgbm>=4.3
catboost>=1.2
gpboost>=1.3           # бустинг + смешанные эффекты (GPBoost)

# --- Визуализация ---
matplotlib>=3.8
seaborn>=0.13

# --- Утилиты ---
tqdm>=4.66             # прогресс-бары при обучении
joblib>=1.3            # параллельное выполнение (зависимость sklearn)

# --- Jupyter (для ноутбуков) ---
jupyterlab>=4.1
ipykernel>=6.29
