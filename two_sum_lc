def mm(num, tar):
    nn = []
    for i in range(len(num)):
        for j in range(i+1, len(num)):
            if len(nn) != 2:
                if num[i] + num[j] == tar:
                    nn.append(i)
                    nn.append(j)
    return nn


print(mm([1, 2, 2, 3, 3, 4, 4], 6))
